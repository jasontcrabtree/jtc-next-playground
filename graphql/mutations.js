import { gql, useMutation } from '@apollo/client';

/* export const ADD_USER = gql`
  mutation addUser($name: String!) {
    insert_mastering_nextjs_one(object: { name: $name }) {
      id
      name
      timeStarted
      isActive
      premium
    }
  }
`; */
